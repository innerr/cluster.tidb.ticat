help = display tiup cluster by name
abbr = disp|dis|di|show|status

[arg]
cluster-name|cluster|name = ''

[arg2env]
tidb.cluster = cluster-name

[env]
tidb.cluster = read

[dep]
tiup = to display cluster
