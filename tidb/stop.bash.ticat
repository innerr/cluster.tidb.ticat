help = stop tiup cluster by name
abbr = stp|down|dn

[arg]
cluster-name|cluster|name|n|N = ''
confirm|cfm|c|C = ''

[arg2env]
tidb.cluster = cluster-name
tidb.op.confirm = confirm

[env]
tidb.cluster = read
tidb.op.confirm = read

[dep]
tiup = to stop cluster
