help = setup br config
tags = config
abbr = br.conf

[arg]
backup-dir|dir|d = ''
cluster-name|cluster|name|n|N = ''

[arg2env]
br.backup-dir = backup-dir
tidb.cluster = cluster-name

[val2env]
tidb.backupper = br
